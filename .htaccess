<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all requests to public/ directory if not already starting with /public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>